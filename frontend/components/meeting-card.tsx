import Link from "next/link"; import {ArrowUpRight,CheckCircle2,Clock} from "lucide-react"; import {Meeting} from "@/types"; import {formatDate,formatDuration} from "@/lib/time";
export function MeetingCard({meeting}:{meeting:Meeting}){return <Link href={`/meetings/${meeting.id}`} className="meeting-card"><div className="card-top"><span className="meeting-icon"><Clock size={17}/></span><ArrowUpRight size={17} className="muted"/></div><h3>{meeting.title}</h3><p>{formatDate(meeting.occurred_at)} · {formatDuration(meeting.duration_ms)}</p><div className="card-bottom"><div className="faces">{meeting.participant_names.slice(0,3).map((name,i)=><span className={`avatar a${i}`} key={name}>{name.split(" ").map(x=>x[0]).join("")}</span>)}</div>{meeting.action_item_count>0&&<span className="task-count"><CheckCircle2 size={14}/>{meeting.action_item_count}</span>}</div></Link>}
